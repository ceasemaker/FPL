# 🔐 Environment Variables for Render Deployment

# ===== DJANGO WEB SERVICE =====
# Copy these to your Django Web Service in Render Dashboard

# Django Core Settings
DJANGO_SECRET_KEY=<generate-with-secrets.token_urlsafe-50>
DEBUG=false
DJANGO_ALLOWED_HOSTS=<your-service>.onrender.com,localhost

# Database (Auto-populated by Render PostgreSQL - use Internal URL)
DATABASE_URL=<from-render-postgres-internal-connection-url>

# Redis (Auto-populated by Render Redis - use Internal URL)
REDIS_URL=<from-render-redis-internal-connection-url>

# Celery Configuration
C_FORCE_ROOT=true

# CORS - Add your frontend URL
CORS_ALLOWED_ORIGINS=https://<your-frontend>.onrender.com,http://localhost:5173

# RapidAPI - SofaSport Integration
RAPIDAPI_KEY=<your-rapidapi-key-from-rapidapi-com>
RAPIDAPI_HOST=sofasport.p.rapidapi.com

# FPL API
FPL_API_BASE_URL=https://fantasy.premierleague.com/api

# Timezone
TZ=Europe/London

# Logging
DJANGO_LOG_LEVEL=INFO

# ===== FRONTEND SERVICE =====
# Copy these to your Frontend Static Site in Render Dashboard

# API URL - Point to your Django service
VITE_API_URL=https://<your-django-service>.onrender.com

# ===== NOTES =====
# 
# 1. DATABASE_URL and REDIS_URL: Use the INTERNAL connection URLs from Render
#    - Go to your PostgreSQL database → Info → Internal Database URL
#    - Go to your Redis instance → Info → Internal Redis URL
#
# 2. DJANGO_SECRET_KEY: Generate a secure key with Python:
#    python -c "import secrets; print(secrets.token_urlsafe(50))"
#
# 3. RAPIDAPI_KEY: Get from https://rapidapi.com/fluis.lacasse/api/sofasport/
#    - Subscribe to the free tier
#    - Copy your API key from the dashboard
#
# 4. CORS_ALLOWED_ORIGINS: Must include your frontend domain
#    - Format: https://your-frontend.onrender.com (no trailing slash)
#    - Multiple origins: separate with commas
#
# 5. DO NOT commit this file with actual values to Git!
